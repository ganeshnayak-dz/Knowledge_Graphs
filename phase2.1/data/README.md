# Data — Phase 2.1

Put shared or framework-specific input data here (CSV, JSON, etc.) so it stays separate from code.

You can use the same datasets as Phase 1/2 (e.g. copy `phase1/data/movie.csv` or `phase2/data/employee.csv`) or add new ones per framework.
